# Outputs

The selected public leaderboard submission is:

```text
submission_stage3_cgc_t1_stage2t2.csv
```

Public score observed during development:

```text
0.53536
```

The CSV is generated by `src/stage3_feature_ensemble.py` during training or by `src/rebuild_submission_from_artifacts.py` from saved artifacts.

